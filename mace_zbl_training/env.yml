# Reuses the existing user-local env: /path/to/conda_envs/gfnff-delta-mace  # TODO: set for your machine
# (Python 3.11, mace-torch, ASE >= 3.23, PyTorch 2.4 + CUDA 12.1).
# This file is a reference only — do not rebuild a fresh env.
#
#   conda activate gfnff-delta-mace
name: gfnff-delta-mace
